import { XCollection } from '../svgs/XCollection';

export const Router: React.FC = () => {
  return (
    <div style={{ paddingLeft: 20, paddingRight: 20 }}>
      <XCollection />
    </div>
  );
};
